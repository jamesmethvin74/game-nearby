name: Temporary post-fix audit result

on:
  push:
    branches: [feature/live-sports-pipeline-m1]
    paths:
      - ".github/workflows/tmp-postfix-audit-result.yml"

permissions:
  contents: read

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Read post-fix statewide audit
        shell: bash
        run: |
          set -euo pipefail
          curl -fsS --max-time 60 "https://statewide-integrity-audit-result-localbleachersar-sports-api.james-methvin74.workers.dev/api/statewide-integrity-audit-result"
          echo
