name: M2 post-retry status capture

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - name: Capture bounded read-only M2 status exactly once
        shell: bash
        run: |
          set -euo pipefail
          status_url="https://localbleachersar-sports-api.james-methvin74.workers.dev/api/v1/_m2-bootstrap-status-20260903-1102"
          status=$(curl -sS -L --max-time 60 \
            -H 'x-localbleachers-m2-bootstrap: approved-certified-1102-20260903' \
            -o m2-post-retry-status.json \
            -w '%{http_code}' \
            "$status_url")
          printf '%s\n' "$status" > m2-post-retry-status-http-status.txt
          cat m2-post-retry-status.json
          if [ "$status" != "200" ]; then
            echo "Status probe returned HTTP $status" >&2
            exit 1
          fi

      - name: Upload post-retry status
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m2-post-retry-status
          path: |
            m2-post-retry-status.json
            m2-post-retry-status-http-status.txt
          if-no-files-found: warn
