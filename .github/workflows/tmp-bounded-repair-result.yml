name: Temporary bounded repair result transport

on:
  push:
    branches: [feature/live-sports-pipeline-m1]
    paths:
      - ".github/workflows/tmp-bounded-repair-result.yml"

permissions:
  contents: read

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Read bounded repair result
        shell: bash
        run: |
          set -euo pipefail
          curl -fsS --max-time 60 "https://bounded-eight-defect-repair-result-localbleachersar-sports-api.james-methvin74.workers.dev/api/bounded-eight-defect-repair-result"
          echo
